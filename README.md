# JavaDB
A  database server capable of parsing and executing a SQL subset. This was a coursework assignement for my degree (the boilerplate client/server setup code was provided for us). This project used Maven, however for now I have just uploaded the src>main Java files.
